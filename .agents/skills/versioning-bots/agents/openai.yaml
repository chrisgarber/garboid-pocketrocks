interface:
  display_name: "Versioning Bots"
  short_description: "Version bots and retain only durable artifacts"
  default_prompt: "Use $versioning-bots to version a bot change and keep its training artifacts lean."
